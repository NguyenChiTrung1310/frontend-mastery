# Reimplement Array Methods

Build `myMap`, `myFilter`, `myReduce`, and `myFlatMap` from scratch using only basic `for` loops.

## Goals
- Understand what each higher-order array method actually does under the hood
- Correctly handle the callback signature (element, index, array)
- Handle edge cases: empty arrays, reducing without initial value
