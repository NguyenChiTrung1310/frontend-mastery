# This Binding Quiz

Five snippets, five different `this` values. Fix each one using `.bind()`, arrow functions, or explicit context passing.

## Goals
- Understand how `this` is determined by call-site, not definition-site
- Know when arrow functions capture lexical `this` vs regular functions
- Fix lost-context bugs in callbacks and event handlers
