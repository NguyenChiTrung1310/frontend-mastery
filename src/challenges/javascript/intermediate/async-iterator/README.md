# Async Iterators & for-await-of

Build an async generator that pages through a simulated API. Use `for await...of` to consume it, implementing backpressure naturally.

## Goals
- Understand how async generators differ from regular generators
- Implement pagination with `yield` and `async/await` together
- Consume an async iterable with `for await...of`
