# Implement Promise.all

Re-implement `Promise.all` from scratch. It should:
- Resolve with an array of results when all promises resolve (in input order)
- Reject immediately when any promise rejects
- Return a resolved promise immediately for an empty array
