# Module Pattern & IIFE

Implement a private counter module using the Revealing Module Pattern (IIFE). The module exposes only the public API while keeping internal state truly private — not accessible from outside.

## Goals
- Understand closures as the mechanism for private state in pre-ES6 JavaScript
- Distinguish between the module pattern and ES modules
- Implement a factory function that returns a public API object
